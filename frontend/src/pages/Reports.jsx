import {useState} from 'react'; import {reports} from '../api'; export default function Reports(){const [f,setF]=useState({period:'MONTH',format:'PDF'}),[rows,setRows]=useState([]);const preview=()=>reports.preview(f).then(r=>setRows(r.data));const download=async()=>{const r=await reports.download(f);const url=URL.createObjectURL(r.data),a=document.createElement('a');a.href=url;a.download=`report.${f.format==='PDF'?'pdf':'xlsx'}`;a.click();URL.revokeObjectURL(url)};return <><h1>Отчёты</h1><select value={f.period} onChange={e=>setF({...f,period:e.target.value})}><option value="MONTH">Месяц</option><option value="QUARTER">Квартал</option><option value="YEAR">Год</option></select><select value={f.format} onChange={e=>setF({...f,format:e.target.value})}><option value="PDF">PDF</option><option value="XLSX">Excel</option></select><button onClick={preview}>Предпросмотр</button><button onClick={download}>Скачать</button><ul>{rows.map(x=><li key={x.id}>{x.transactionDate} — {x.amount}</li>)}</ul></>}