import {useState} from 'react'; import {auth} from '../api'; export default function Register(){const [f,setF]=useState({name:'',email:'',password:'',confirmPassword:''}),[m,setM]=useState('');const submit=async e=>{e.preventDefault();try{await auth.register(f);setM('Пользователь зарегистрирован. Для полной SPA-аутентификации добавьте JWT/login endpoint.')}catch(e){setM(e.response?.data?.error||'Ошибка регистрации');}};return <><h1>Регистрация</h1><form onSubmit={submit}>{Object.keys(f).map(k=><input key={k} type={k.includes('password')||k==='confirmPassword'?'password':'text'} placeholder={k} value={f[k]} onChange={e=>setF({...f,[k]:e.target.value})}/>) }<button>Зарегистрироваться</button></form><p>{m}</p></>}