import {useEffect,useState} from 'react'; import {categories} from '../api'; export default function Categories(){const [items,setItems]=useState([]),[f,setF]=useState({name:'',type:'EXPENSE'});const load=()=>categories.list().then(r=>setItems(r.data));useEffect(load,[]);const add=async e=>{e.preventDefault();await categories.create(f);setF({name:'',type:'EXPENSE'});load();};return <><h1>Категории</h1><form onSubmit={add}><input required value={f.name} placeholder="Название" onChange={e=>setF({...f,name:e.target.value})}/><select value={f.type} onChange={e=>setF({...f,type:e.target.value})}><option value="EXPENSE">Расход</option><option value="INCOME">Доход</option></select><button>Добавить</button></form><ul>{items.map(x=><li key={x.id}>{x.name} ({x.type}) <button onClick={async()=>{await categories.remove(x.id);load()}}>Удалить</button></li>)}</ul></>}